- Try to move cards that have the exact same fields content, not based on GUID

    This could move cards wrongly if uses lesser fileds, with more field matches is more
    likely to be the same even if its has different GUID